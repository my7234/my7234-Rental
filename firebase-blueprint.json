{
  "entities": {
    "Property": {
      "title": "Property",
      "description": "A rental property listing",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "Title of the listing" },
        "type": { "type": "string", "enum": ["Home", "Shop", "Apartment"] },
        "stories": { "type": "string", "enum": ["Single", "Double"] },
        "rooms": { "type": "string" },
        "bathrooms": { "type": "string" },
        "country": { "type": "string" },
        "city": { "type": "string" },
        "area": { "type": "string" },
        "address": { "type": "string" },
        "hasGas": { "type": "boolean" },
        "hasElectricity": { "type": "boolean" },
        "phone": { "type": "string" },
        "whatsapp": { "type": "string" },
        "currency": { "type": "string" },
        "language": { "type": "string" },
        "imageUrls": { "type": "array", "items": { "type": "string" }, "description": "Array of 3 image URLs" },
        "isAvailable": { "type": "boolean", "description": "Is the property currently available?" },
        "createdAt": { "type": "integer" },
        "ownerEmail": { "type": "string" },
        "ownerUid": { "type": "string" }
      },
      "required": ["title", "type", "stories", "rooms", "bathrooms", "country", "city", "area", "address", "phone", "whatsapp", "currency", "language", "ownerUid"]
    },
    "User": {
      "title": "User",
      "description": "App user profile for stats and identification",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "lastLogin": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    }
  },
  "firestore": {
    "/properties/{propertyId}": {
      "schema": "Property",
      "description": "Collection of all rental properties"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "Collection of registered users"
    }
  }
}
